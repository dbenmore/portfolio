{
  "name": "portfolio",
  "description": "",
  "scripts": {
    "clean": "rm -rf docs",
    "build:scss": "sass ./sass:./build",
    "build": "npm run clean && npm run build:scss && npx @11ty/eleventy",
    "start": "npm run clean && npm run build:scss && eleventy --serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "clean-css": "^5.3.0",
    "sass": "^1.50.1"
  }
}
